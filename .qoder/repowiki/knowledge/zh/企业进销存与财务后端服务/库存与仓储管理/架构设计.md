- 采用 Express Router 定义 RESTful 接口，通过 `authMiddleware` 统一拦截鉴权。
- 依赖 `server/db/init.js` 获取 SQLite 数据库实例，直接执行 SQL 进行数据持久化。
- 业务逻辑分层：路由层处理参数校验，事务层（`db.transaction`）保障出入库原子性，同时更新总库存表 `products` 与分仓库存表 `product_warehouse_stock`。
- 数据模型关联：商品关联公司（`companies`），入库关联公司与库房，出库关联客户（`clients`）与库房。
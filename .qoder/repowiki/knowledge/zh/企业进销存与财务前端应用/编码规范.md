- 所有页面组件均采用函数式组件配合 React Hooks（useState, useEffect）进行状态管理与副作用处理。
- API 响应遵循统一的 `{ code, data, message }` 结构，前端通过判断 `code === 0` 确认业务逻辑成功。
- 列表页普遍采用“搜索栏 + Card 包裹的 Table”布局模式，并集成服务端分页与关键词过滤功能。
- 复杂表单操作（如入库、出库、编辑）均通过 Ant Design Modal 承载，并利用 `Form.useForm` 实例控制字段值。
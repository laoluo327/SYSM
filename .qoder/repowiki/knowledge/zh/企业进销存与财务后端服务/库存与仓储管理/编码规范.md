- 所有路由模块均强制挂载 `authMiddleware` 中间件以保护接口安全。
- 出入库批量操作均包裹在 `db.transaction` 中，任何一项失败即回滚全部变更。
- 删除核心实体（如商品、库房）前必须检查是否存在关联的库存记录或流水记录。
- 列表查询统一支持 `keyword` 模糊搜索及 `page/pageSize` 分页参数，返回格式固定为 `{ code, data: { list, total } }`。
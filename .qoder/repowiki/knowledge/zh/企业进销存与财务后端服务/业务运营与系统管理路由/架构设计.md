- 采用 Express Router 模块化设计，每个业务实体（如 clients, companies）对应独立的路由文件。
- 依赖注入模式：通过 `../db/init` 获取数据库实例，通过 `../middleware/auth` 引入认证与权限中间件。
- 权限控制分层：大部分路由全局启用 `authMiddleware`，写操作或敏感数据进一步启用 `adminMiddleware`。
- 数据访问层内联：SQL 查询直接嵌入路由处理函数中，未抽象出独立的 Service 或 Repository 层。
- 统一响应格式：所有接口均返回包含 `code`（0 表示成功）、`data` 和可选 `message` 的 JSON 结构。
- 分页查询规范：列表接口统一支持 `page` 和 `pageSize` 查询参数，并返回包含 `total` 的分页对象。
- 软性查重机制：在创建或更新资源时，普遍执行 SQL 查询以检查名称或账号的唯一性，防止重复数据。
- 输入 sanitization：对字符串类型的输入普遍使用 `.trim()` 处理，并对空值提供默认空字符串或错误提示。
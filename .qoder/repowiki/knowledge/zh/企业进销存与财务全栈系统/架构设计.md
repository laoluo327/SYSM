- 采用前后端分离架构，前端（client）通过 RESTful API 与后端（server）通信。
- 后端基于 Express 构建，通过中间件（如 `middleware/auth.js`）实现统一的身份验证与权限控制。
- 使用 SQLite 作为共享持久层，各业务路由（如库存、商品、财务）直接操作同一数据库实例，确保数据一致性。
- 前端通过角色化布局（Admin/User Layouts）隔离不同用户的视图与操作权限，与后端鉴权逻辑保持一致。